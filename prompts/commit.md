Create one clean commit from the current changes.

Requirements:
- One commit, not several.
- Commit message is short but informative. Angular commit message style: feat(component), fix, test, docs, refactor, style, chore.
- If there are tests - run them.
